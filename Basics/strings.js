let strM = "Manojit"
console.log(strM.length);
console.log(strM[1]);

let pen = {
    name: "pentech",
    cost: 10
};

let op = `cost of pen is ${pen.name} is ${pen.cost}`;

// Printing using Literals
console.log( ` cost of pen is ${pen.name} is ${pen.cost}`);



